extends OS
# Extended operating system functions.


func execute(path: String, arguments: PoolStringArray, blocking: bool = false,
			output: Array = [], read_stderr: bool = false, stream: StreamPeer = StreamPeer.new()) -> int:
		return 0
